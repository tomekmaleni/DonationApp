import {StyleSheet} from 'react-native';

const globalStyle = StyleSheet.create({
  backgroundWhite: {
    backgroundColor: '#fff',
  },
  flex: {
    flex: 1,
  },
});

export default globalStyle;
